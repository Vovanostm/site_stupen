RewriteEngine on


<IfModule mod_rewrite.c>
    RewriteEngine on
    RewriteBase /
    RewriteCond %{HTTP_HOST} ^арт-ступень
    RewriteRule (.*) http://www.арт-ступень.рф/$1 [R=301,L]
    RewriteCond %{THE_REQUEST} ^[A-Z]{3,9}\ /index\.php\ HTTP/
    RewriteRule ^index\.php$ http://www.арт-ступень.рф/ [R=301,L]
</IfModule>